define(['crafty','game/utils', 'game/entities/enemy'], function (Crafty, Utils, Enemy) {

    return {
        init : function (max) { 
            
            //Crafty.e('Enemy, EnemyFighter');
        },
        spawn: function () {
            
        }

    };
});